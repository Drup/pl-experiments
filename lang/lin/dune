(ocamllex lexer)
(menhir
  (modules parser)
  (flags --explain --strict)
)

(executable
  (name affe)
  (libraries ocamlgraph sequence zoo)
  (preprocess (pps ppx_deriving.std))
)

(alias
 (name   runtest)
 (action (run ./affe.exe %{deps}))
 (deps (glob_files "examples/*.affe"))
)