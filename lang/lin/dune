(ocamllex lexer)
(menhir (modules parser))

(executable
  (name lin)
  (libraries ocamlgraph zoo)
)

(alias
 (name   runtest)
 (action (ignore-stdout (run ./lin.exe %{deps})))
 (deps (glob_files "*.lin"))
)